// length();
// The length property sets or returns the numbers of elements in an array.
// Syntax := array.length;

const arr = [1,2,3,4,5,6];
console.log(arr.length) //6

arr.length = 9;
console.log(arr); // (9) [1, 2, 3, 4, 5, 6, empty × 3]