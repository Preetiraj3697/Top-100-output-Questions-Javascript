// isArray();

// The isArray() method determines whether an object is an array. This function returns true if the object
// is an array, and false if not.

// Syntax := Array.isArray(obj);

// obj(Required) := The object to be tested.

// Array.isArray();
console.log(Array.isArray([1,2,3])) // true
console.log(Array.isArray({abc:123}))  // false